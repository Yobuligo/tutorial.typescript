/**
 * The following code shows how to ensure that an object has a specific property
 */
/**
 * The following function returns a property of an object by the properties key
 * Therefor
 * 1. T must be of type of an object (so extends object)
 * 2. the property key [K] must be of part of the object. This can be checked by K extends KEYOF T
 */
function getProperty(object, key) {
    return object[key];
}
//# sourceMappingURL=app.js.map